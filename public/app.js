const API=window.YONGRYEON_API_BASE;const works=document.getElementById('works');const empty=document.getElementById('empty');
async function load(){try{const r=await fetch(`${API}/api/artworks`);const data=await r.json();empty.style.display=data.length?'none':'block';works.innerHTML=data.map(a=>`<a class="card" href="viewer.html?id=${a.id}"><div class="card-img">${a.image_url?`<img src="${a.image_url}" alt="${a.title}">`:'No Image'}</div><h2>${a.title}</h2><p>${[a.material,a.year].filter(Boolean).join(' · ')}</p></a>`).join('')}catch(e){empty.textContent='API 연결이 필요합니다. public/config.js의 Worker 주소를 확인하세요.'}}load();
